export const initialState={
    breakLength:5,
    sessionLength:25,
    timer:25*60,
}
function pomodoReducer(state, action) {
  switch (action.type) {
    default:
      return state;
  }
}

export default pomodoReducer;
